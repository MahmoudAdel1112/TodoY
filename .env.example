# MongoDB Connection
# Development: mongodb://localhost:27017/todos-app
# Production: mongodb+srv://username:password@cluster.mongodb.net/todos-app?retryWrites=true&w=majority
MONGO_URI=mongodb://localhost:27017/todos-app

# JWT Configuration
# IMPORTANT: Change this to a strong, random value in production!
# Generate a secure secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=90d

# Server Configuration
PORT=8000
NODE_ENV=development

# CORS Configuration
# Development: Leave commented or set to *
# Production: Set to your specific frontend URL (e.g., https://yourfrontend.com)
# Multiple origins: https://app1.com,https://app2.com
# CORS_ORIGIN=http://localhost:3000
